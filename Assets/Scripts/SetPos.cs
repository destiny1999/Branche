using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SetPos : MonoBehaviour
{
    // Start is called before the first frame update
    void Awake()
    {
        /*this.gameObject.transform.Find("people1 (1)").transform.position =
            this.gameObject.transform.Find("people1").transform.position;
        this.gameObject.transform.Find("people2 (1)").transform.position =
            this.gameObject.transform.Find("people2").transform.position;
        this.gameObject.transform.Find("people3 (1)").transform.position =
            this.gameObject.transform.Find("people3").transform.position;*/
    }

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
